---
import { resolveLocale, localizedUrl, appUrl, t } from '../i18n/t';

const locale = resolveLocale(Astro.currentLocale);
---
<section class="cta">
  <div class="container">
    <div class="cta-card">
      <span class="eyebrow eyebrow--steel">{t('cta.eyebrow', locale)}</span>
      <h2>{t('cta.heading', locale)}</h2>
      <p>{t('cta.lede', locale)}</p>
      <div class="cta-buttons">
        <a href={appUrl('/signup', locale)} class="btn btn-primary btn-lg">{t('cta.primary', locale)}</a>
        <a href={localizedUrl('/contact/', locale)} class="btn btn-ghost btn-lg">{t('cta.secondary', locale)}</a>
      </div>
    </div>
  </div>
</section>
